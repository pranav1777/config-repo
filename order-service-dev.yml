global:
  message: Welcome to the order service environment

server:
  port: 8084

message: Hello from order developer

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ordermaster
    username: root
    password: pranav

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
