server:
  port: 8084

message: Hello from order developer

spring:
  datasource:
    url: jdbc:h2:mem:orderdb
    username: sa
    password: ""
    generate-unique-name: false

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
