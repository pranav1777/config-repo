spring:
  application:
    name: order-service

  datasource:
    url: jdbc:mysql://localhost:3306/ordermaster
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
