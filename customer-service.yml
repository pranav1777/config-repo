spring:
  application:
    name: customer-service

  datasource:
    url: jdbc:mysql://localhost:3306/customermaster
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
